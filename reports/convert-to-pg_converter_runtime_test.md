# Convert To pg_converter_runtime_test

## Phạm Vi

- Database duy nhất được sử dụng: `pg_converter_runtime_test`
- Test server: `221.148.141.4:5432`
- Không sử dụng production database.
- Không sửa source/converter.

## PostgreSQL Environment

- Database name: `pg_converter_runtime_test`
- Connected user: `postgres`
- PostgreSQL server: `PostgreSQL 9.3.17, compiled by Visual C++ build 1600, 64-bit`
- `psql`: `9.5.5`
- `pg_dump`: `9.5.5`
- Connection và database identity check: **PASS**

## Backup Và Cleanup

Backup trước khi cleanup:

- `reports/backups/pg_converter_runtime_test_before_convert_20260629_094401.sql`
- Backup result: **PASS**

Cleanup:

- Có guard xác minh `current_database() = 'pg_converter_runtime_test'`.
- Chỉ drop/recreate schema `public` trong database test.
- Không xóa hoặc thay đổi database khác.
- Cleanup result: **PASS**

## Build

Command:

```powershell
dotnet build
```

Result: **PASS**

- Warnings: `0`
- Errors: `0`

## QA

Command:

```powershell
dotnet run --project $env:TEMP\PgConverterQaRunner2\PgConverterQaRunner.csproj
```

Result: **PASS**

- Total checks: `27`
- PASS: `27`
- FAIL: `0`

## Converter Output

Converter/QA runner đã sinh lại:

- MSSQL input: `reports/generated/sample-mssql-input.sql`
- PostgreSQL output: `reports/generated/converted-output.sql`

Generated output result: **PASS**

## SQL Files Đã Chạy

1. `reports/generated/converted-output.sql`
2. `reports/runtime-table-smoke-test.sql`
3. `reports/runtime-orders-smoke-test.sql`
4. `reports/convert-runtime-verification.sql`

Main output command dùng:

```text
psql -v ON_ERROR_STOP=1 -f reports/generated/converted-output.sql
```

Main output exit code: `0`

Logs:

- `reports/convert-to-pg-runtime.stdout.log`
- `reports/convert-to-pg-runtime.stderr.log`

## Object Validation

Generated logical files/objects:

- Total: `12`
- PASS: `12`
- FAIL: `0`

Catalog validation:

| Object | Expected | Actual | Status |
|---|---:|---:|---|
| TABLE | 2 | 2 | PASS |
| Explicit INDEX | 6 | 6 | PASS |
| Total catalog INDEX, including PK | 8 | 8 | PASS |
| FUNCTION | 2 | 2 | PASS |
| PRIMARY KEY | 2 | 2 | PASS |
| FOREIGN KEY | 1 | 1 | PASS |
| Stored procedure object | 0 | 0 | PASS, warning-only by design |

Objects confirmed:

- Tables: `Users`, `Orders`
- Functions: `fn_userlabel(character varying, boolean)`, `fn_userorders(integer)`
- FK: `fk_orders_users`
- Indexes: `ix_users_name`, `ux_users_guid`, `ix_orders_user_amount`,
  `ix_users_active`, `ix_orders_user_include`, `cx_users_tenant`
- PK indexes: `Users_pkey`, `Orders_pkey`

## Smoke Tests

| Test | Result |
|---|---|
| Insert sample row into `public."Users"` | PASS |
| Verify identity and `isactive = true` default | PASS |
| Insert sample row into `public."Orders"` through FK | PASS |
| Call `fn_userlabel('QA', TRUE)` | PASS |
| Call `fn_userorders(1)` | PASS |
| Verify table rows, indexes, functions and FK counts | PASS |

Runtime values:

- `Users`: `1` row
- `Orders`: `1` row
- `fn_userlabel('QA', TRUE)`: `QA Active`
- `fn_userorders(1)`: `(orderid=1, amount=25.50)`

Smoke test result: **PASS**

## Errors And Notices

PostgreSQL execution errors: **0**

Two expected NOTICE messages occurred:

- Existing `fn_userlabel` signature did not exist during `DROP FUNCTION IF EXISTS`.
- Existing `fn_userorders` signature did not exist during `DROP FUNCTION IF EXISTS`.

These NOTICE messages did not stop execution; exit code remained `0`.

One initial catalog-count command had a PowerShell identifier-quoting issue.
It was a test-wrapper issue, not converter SQL. The same validation was rerun
from `reports/convert-runtime-verification.sql` and passed.

## Remaining Limits

- Stored procedures are warning-only because PostgreSQL 9.3 does not support `CREATE PROCEDURE`.
- Dynamic SQL and complex procedure semantics still require manual rewrite.
- `NEWID()` requires manual rewrite or an explicitly verified UUID extension.
- Filtered indexes, included columns and clustered-index semantics still require review.
- Passing this suite does not prove correctness for every T-SQL input.

## Conclusion

**PASS theo bộ test hiện tại**

PostgreSQL output generated by the converter executed successfully on
`pg_converter_runtime_test` using PostgreSQL 9.3.17. All expected test
objects were present and all current smoke tests passed.

Không kết luận converter đúng 100% mọi trường hợp.
