-- ─── TABLE: EAPPDecisioner ───────────────────────────────────
CREATE TABLE IF NOT EXISTS public."EAPPDecisioner" (
    Decisioner text,
    UserID text,
    UseName text,
    ID double precision
);
-- TODO: Owner mapping skipped. Target role postgres not verified.
