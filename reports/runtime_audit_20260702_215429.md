# Runtime converter audit - 2026-07-02 21:54 +07:00

## Result

| Status | Before | After |
|---|---:|---:|
| Runtime PASS | 227 | Not rerun |
| Runtime FAIL | 28 | Not rerun |
| BLOCKED | 99 | Not rerun |

No runtime status changed or was inferred. The last fully verified baseline is
227 PASS / 28 FAIL / 99 BLOCKED from 354 discovered routines.

## Root-cause review

- Two routines contain malformed dynamic SQL with a semicolon immediately
  before a following `LEFT JOIN`.
- One Board INSERT still emits integer `1` for an `Enabled` boolean column.
- The remaining recorded failures are primarily dummy/seed-data constraints,
  missing helpers or relations, XML shredding, dynamic parameter binding, and
  source-specific type mismatches.

These candidates were not changed because the required runtime redeploy and
smoke gate cannot currently run.

## Validation

- `dotnet build pg_converter_ui.csproj -c Release`: PASS, 0 warnings/errors.
- Full NUnit Regression: PASS, 77/77.
- Board QA: PASS, 24/24.
- Full runtime smoke: attempted, blocked before connection because
  `PG_RUNTIME_CONNECTION` is not configured.
- Runtime source redeploy: unavailable because `MSSQL_SOURCE_CONNECTION` is
  also not configured.

## Changes and regressions

- Converter files changed: none.
- Regression tests added: none.
- Procedures rescued: 0.
- Commit: none; runtime validation gate was not satisfied.
- Existing untracked `scratch/view_pg_func.ps1`: preserved and untouched.

## Next step

Configure `PG_RUNTIME_CONNECTION` for `pg_converter_runtime_test` and
`MSSQL_SOURCE_CONNECTION`, reproduce the 227/28/99 baseline, then implement and
validate a general rule that removes statement terminators embedded between
dynamic SQL JOIN clauses. Follow with the boolean INSERT case if its source
shape confirms a general parser gap.
